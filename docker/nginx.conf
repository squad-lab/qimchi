server {
    listen 80 default_server;
    server_name localhost;

    # Serve static frontend
    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # TODONOW:
    # Proxy backend endpoints (no /api/ prefix in backend). Match common backend route prefixes
    # and forward them to the FastAPI/Gunicorn server at 127.0.0.1:8000
    # location ~ ^/(?:health|plot|download|download-selected|download-multiple|download-folder|export-plot-images|apply-filters|apply-sliders|load-notes|save-notes|load|load-attrs|load-meta|dataset-status|filters|notes|dirtree)(?:/|$) {
    #     proxy_pass http://127.0.0.1:8000;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    #     proxy_redirect off;
    # }
}
